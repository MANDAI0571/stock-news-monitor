{"name":"Apply Fix 15","on":{"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"apply":{"runs-on":"ubuntu-latest","timeout-minutes":30,"steps":[{"uses":"actions/checkout@v7"},{"uses":"actions/setup-python@v6","with":{"python-version":"3.11"}},{"name":"deps","run":"python -m pip install --upgrade pip && pip install -r requirements.txt"},{"name":"patch","run":"base64 -d tools/fix15.b64 | gunzip > tools/p15.py && python3 tools/p15.py && rm -f tools/p15.py && git rm -q tools/fix15.b64 && git --no-pager diff --stat HEAD"},{"name":"selftest-gate","run":"python3 -c \"import sys;sys.path.insert(0,'.');import self_test;self_test._test_note_split_passes_quality_gate()\""},{"name":"selftest-full","run":"python3 self_test.py"},{"name":"commit","run":"git config user.name 'github-actions[bot]' && git config user.email 'github-actions[bot]@users.noreply.github.com' && git add validate_note_artifact.py self_test.py && git commit -m 'fix: validate split note drafts as one article' && git pull --rebase origin main && git push origin HEAD:main"},{"name":"notify",if: failure(),"env":{"GMAIL_USER":"${{ secrets.GMAIL_USER }}","GMAIL_APP_PASSWORD":"${{ secrets.GMAIL_APP_PASSWORD }}","MAIL_TO":"${{ secrets.MAIL_TO }}"},"run":"python3 notify_workflow_failure.py"}]}}}
