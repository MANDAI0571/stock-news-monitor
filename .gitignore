__pycache__/
scanner/__pycache__/
outputs/*.csv
outputs/*.json
outputs/*.out
outputs/*.err
cache/
launchd/

# note.com 認証セッション（絶対にコミットしない＝漏洩防止）
note_state.json
note_state.b64
outputs/note_autosave_error.png

# local generated outputs
# （outputs/ ではなく outputs/* にしているのは、performance_report.md だけ
#   git の追跡対象に戻すため。ディレクトリごと無視すると ! が効かない）
outputs/*
!outputs/performance_report.md
