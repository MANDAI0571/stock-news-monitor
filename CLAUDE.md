# stock-news-monitor — 作業の決まりごと

このファイルは Claude Code が毎回読む。ここに書いてあることは守る。

作者: 高重（AI投資研究所株式会社）。Mac は初心者。専門用語は避け、日本語で書く。

---

## 1. 高重さんとの約束（最優先・変更禁止）

1. **捏造禁止。** 実際にログ・メール・ページを開いてから報告する。開いていないものは「未確認」と書く。
   推測は「〜と考えられます（推定）」と明示する。
2. **免責必須。** スクリーニングや相場に関する成果物の末尾に
   「本記事は投資助言ではありません。売買判断はご自身の責任でお願いします。」を必ず入れる。
3. **公開ゲート。** note への投稿、外部への送信は、高重さんの承認なしには絶対にしない。
   メールの宛先にある `tanto571@yahoo.co.jp` は**社外の人間**。
4. **簡潔に。** 成果物を先に出す。前置きと後書きは最小限。
5. **完了の定義。** 「できた」と言っていいのは次の3つが揃ったときだけ。
   (1) origin/main に入った (2) 本番のログで動いたのを自分の目で見た (3) 相手の手元に届いた
6. **報告の型。**「できたこと（証拠つき）／できていないこと（何が邪魔しているか）／次にやること」の3つ。
7. **自分でやる。** 高重さんにコマンドを渡さない。自分で最後まで実行する。
   できないときは「できません」と正直に言い、理由を書く。言い訳や遠回しな言い方はしない。

## 2. コードを直すときのやり方

- **1つずつ直す。** まとめて直さない。1つ直したら検証してから次へ。
- **修正スクリプトは冪等にする。** 同じものを2回流しても結果が変わらないこと。
  `fixNN.py` の形式で、文字列の置き換えは前後を含めたアンカーで行い、
  適用済みかどうかは「置換後の文字列が既にあるか」で判定する。
- **スクリプト自身に確認を入れる。** 直したあとで
  `ast.parse()`（構文が壊れていないか）と `MUST_EXIST` / `MUST_BE_GONE` の確認をする。
- **検証は必ず main の新しいクローンで。**
  `git clone --depth 1 https://github.com/MANDAI0571/stock-news-monitor.git`
- **コメントは日本語で、なぜそう直したかを書く。** 「何をしたか」はコードを読めば分かる。

### 自己テストの回し方

`self_test.py` を直接動かすと、`_test_9256_limit50_excluded_but_full_universe_included`（JPXへの通信が必要）
で止まることがある。手元では `_test_*` 関数を1つずつ呼び、この1つだけスキップする。
正常なら **PASS=50 FAIL=0 SKIP=1**。

CI（GitHub Actions）では `python3 self_test.py` を丸ごと流すので、通信テストも走る。

## 3. この環境でできないこと（時間を無駄にしないため）

- **`git push` は使えない。** サンドボックスに GitHub の認証情報がない。
  書き込みは **`tmp_apply` ワークフロー**（Actions → tmpapply → Run workflow）に
  修正スクリプトを gzip+base64 にして渡す。
- **`GITHUB_TOKEN` では `.github/workflows/` を変更できない。**
  ワークフローの作成・編集・削除は必ず GitHub の Web 画面から。
  また GITHUB_TOKEN で push しても他のワークフローは起動しない。
- **通信がブロックされているドメイン**（回り込もうとしない）:
  news.google.com / nomura.co.jp / dlri.co.jp / nikkei.com / kabutan / jiji /
  jpx.co.jp / openwork.jp / api.github.com / Yahoo Finance。
  届くのは github.com、note.com、PyPI、Gmail連携、mandai0571.github.io。
  WebSearch は使える。

## 4. note.com について（実物で確認ずみ）

**note の本文欄はマークダウンを一切解釈しない。**
`#` `##` `**` バッククォート `<!-- -->` **表（`|`）**、すべてそのまま文字として表示される。

- 記事の中でリンクを書くときは **裸のURL**。note が自動でリンクにする。
  `[文言](URL)` の形式は押せない文字列になる。
- コピー用ページとコピー用メールの本文は `note_mail_html.py` の `note_body_text()` を通し、
  記号を落とし、表を「1行目=名前とコード / 2行目=項目 値」の並びに組み替えてから渡す。
- メールとコピー用ページの**プレビュー**は `render_note_html()` で本物の表として描く。
  組み替えるのは「note に貼る本文」だけ。

## 5. 毎日の流れと、どのファイルが何をするか

| 時刻(JST) | ワークフロー | 中身 |
|---|---|---|
| 08:41〜13:07 | Intraday High Alert | ザラ場の高値アラート（`intraday_high_alert.py`） |
| 09:40 | Claude 300man Open Fill | 前日宣告した注文を寄付きで約定させる |
| 16:10 | Trade Verification | シグナルを記録し、過去シグナルを採点する |
| 16:30 / 17:00 / 17:30 | Note下書き（クラウド生成） | 記事3本の生成 → コピー用ページ → メール |
| 07:30 | Daily Discipline Screening | 規律スクリーニング（メールは手動実行時のみ） |

- 記事の本体は `note_draft.py`。3本（claude / pullback / highs）を `outputs/note_*.md` に書く。
- メールとコピー用ページは `cloud_mail_digest.py` + `note_mail_html.py`。
- 品質ゲートは `validate_note_artifact.py`。300万円運用の記事は
  `_300man_journal.csv`（本物の台帳）に触れていないと落とす。
  これは **2026-07-16 に、買っていない銘柄を保有として配信した事故**の再発防止。
- **note.com への下書き自動保存は 2026-09-03 に停止した**（`note_draft_cloud.yml` の
  「Save 3-note drafts」ステップに `if: false`）。手で保存したい日は `Note Autosave` を手動実行する。

## 6. 300万円運用（claude勘定）

- 台帳は `data/claude_300man_orders.csv`（注文）と `data/claude_300man_journal.csv`（約定）。
  **記事に載せる保有・損益は、必ずこの2つから作る。** スクリーニング結果を保有として書かない。
- 規律: 1枠100万円・最大3銘柄 / 損切 -7% / 利確 +15% / 10営業日で手じまい。
- **銘柄の選び方はチャートと出来高だけ**（`scanner/scoring.py` の `score_stock`）。
  ニュース本文や業績予想は使っていない。決算は「発表日が確認できているか」だけを見て、
  未確認なら S ランクを A に落とす。記事にもそう書く。
- 買付を決めた時点で、当てはまった条件を注文台帳の `reason` に `｜` 区切りで残す
  （`claude_300man_declare.py`）。記事はそれを読者の言葉に言い換えて出す。

## 7. 過去にやらかしたこと（同じ轍を踏まない）

- **2026-08-28 fix33**: 深夜にメールが届く問題を直そうとして「JST 16〜20時の外なら走らせない」
  という門番を入れた。GitHub の起動が毎回7時間以上遅れていたため、**8/28〜9/1 は1通も届かなくなった**。
  時刻で止める門番を入れるときは、**遅延した場合に何が起きるか**を必ず考える。
- **2026-07-16**: 買っていない銘柄を保有として配信した。以降 `validate_note_artifact.py` が
  台帳マーカーを確認している。
- **2026-09-03 JPX**: 銘柄一覧が `.xls` から `.xlsx` に変わり、旧URLが 404 になって BUY検証が落ちた。
  `scanner/universe.py` の `JPX_LISTED_URLS` は上から順に試すので、新しい形式を先頭に置く。
  キャッシュ（`cache/jpx_listed.csv`）がある間は表面化しないので、**気づくのが遅れる**。
- **GitHub Actions の定時実行は遅れる。** 数時間遅れることがある。
  `note_draft_cloud.yml` は 16:30 / 17:00 / 17:30 の3本立てで取りこぼしを拾い、
  その日のコピー用ページが既にあれば後続は guard で止まる。

## 8. 書き方（記事も、報告も）

- 数字は根拠のあるものだけ。取れなかったら「未取得」「データ不足」と書く。埋めない。
- 読者に何も伝えない行は書かない（例:「OpenWork：取得できず」を毎行入れない）。
  ただし損益のように「無いこと自体が情報」のものは明示する。
- 専門用語は読者の言葉に置き換える（「MA25上」→「25日線の上」）。
- 同じ銘柄を1つの記事に何度も出さない。まとめて1回にする。
